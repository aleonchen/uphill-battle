# TODO · 工作清单（临时方案文件）

用途：候选方案的暂存与筛选。定了方向的排在前头，做完一项划掉一项。
长期决策与踩坑记录仍在 `PLANS.md`，本文件只放"待办"。

## 里程碑 A：多人联网（已排期，方案已定）

架构锁定 **host-relay**：房主浏览器跑完整 Game（AI/弹道/回合），加入者发输入、收快照、只渲染。
协议先 JSON 后二进制；客户端只认一个 WS 地址，本地/线上零改动切换。

- [x] **阶段 0：冻结与抽象**（0.5 天）
  - 冻结玩法：里程碑 A 期间不加新系统
  - `player.js` 输入抽象为 `InputState`（移动/视角/开火/投掷/用药/交互），
    本地玩家与网络玩家共用同一入口——联网地基（随里程碑 C 触屏完成）
- [x] **阶段 1：本地中继 + 同机双开**（1~1.5 天，硬骨头）
  - `ws-relay.js`（Node 零依赖）：房间 + 转发，无游戏逻辑
  - `js/net.js`：`?room=xxx` 进房，先进者为房主；房主 20Hz 快照，加入者发输入
  - 验证：同机两个标签页互见移动/开枪/中弹（`tools/net-probe.mjs`，TEST-NET）
- [x] **阶段 2：局域网真人局**（0.5~1 天）
  - 手机/另一台电脑连 `ws://局域网IP` 真人对枪（serve.py/relay 均绑 0.0.0.0，已验证 192.168.x 可达）
  - 远端实体 100ms 插值缓冲、本地即时曳光（预测+host 确认去重）、断线/房主离开提示
  - 远端开载具（按司机拆输入+共享上下车）、投掷物飞行体/烟团同步
  - 验证：E2E 7 verdict 全过（含载具/投掷物）；真机手感待用户 WiFi 实测
- [x] **阶段 3：CF 上线（代码完成，待部署）**（1 天）
  - 中继搬进 Durable Object（`cf-worker/`，每房间一个 DO，协议同构）
  - 大厅 UI：创建/加入/快速匹配/复制邀请链接（`?room=CODE` 直达）
  - 进入方案定论：私密房号+邀请链接为主，PUB 公共房快速匹配为辅
  - 验证：miniflare 协议 7 断言 + 全栈 E2E 7 verdict 全过
  - **待用户执行**：`cd cf-worker && npx wrangler login && npx wrangler deploy`（需 CF 账号）
- [ ] **真人对局验收**（用户 WiFi/朋友局）：手感、平衡、房主离开体验

后置（按需，先不做）：客户端预测（自己移动零延迟感）、服务器权威防作弊
（需把 game.js 剥出 DOM，工作量大一档，朋友局用不上）。

**已拍板（2026-08-02）**：
1. 人类替换 bot 坑位（2 人 + 2 bot vs 4 bot，俩人即可开局）✓ 已实现
2. 房主权威（快，但房主卡=全房卡）✓ 已接受；房主离开不迁移，加入者提示"比赛结束"

## 里程碑 C：iPad / 触屏适配（主体完成，剩真机打磨）

核心思路：不重做一套，给输入层加一个"触摸输入源"——与里程碑 A 阶段 0 的
`InputState` 是同一块地基（先做这个，联网也受益）。

输入模式判定（已定论）：**last-input-wins + 手动兜底**，不做设备探测
（`maxTouchPoints` 在 iPad 配键盘 / Surface 触屏本上会误判——"有能力"不等于"正在用"）。
Pointer Events 的 `pointerType`（touch/mouse/pen）是干净信号：最后用到的输入决定模式，
游戏中可无缝切换；开始界面留"自动/触屏/键鼠"开关 + localStorage 记忆 + `?touch` 强制参数。
Pointer Lock 仅键鼠模式请求；触屏模式拖拽转视角 + 触屏版暂停提示。

- [x] **输入层**：`js/input.js` 通用 InputState + 事件队列 + 键盘/触控双源
  （手柄未来同构）；`player.js` 已重构为纯消费输入层
- [x] **触控 UI**：左下虚拟摇杆（模拟量/推满疾跑）、右半屏滑屏视角、
  右侧按钮集群（开火/瞄/跳/换弹/雷/烟）
- [x] **HUD 可点化**：快捷栏格子（吃药/丢雷）、武器名（切枪）、背包面板（本就可点）
- [x] **触屏补齐**：换枪按钮（轮换）、上/下车上下文按钮（近车浮现，CDP 实测通过）
- [x] **布局与性能**：viewport-fit=cover、触屏 pixelRatio 1.5、静音键避位、`?touch` 调试
- [x] 验证：`?test=input` 双源断言全过 + 触屏 UI 截图
- [ ] 真机试玩打磨（灵敏度/按钮位置/上车救援按钮浮现时机；触屏缺 V 画风/M 静音键）

## 里程碑 B：单机打磨候选（未排期，按心情/反馈选）

- [ ] 帧率实测与优化（MC 模式 7 万方块 + 2048 阴影 + 419 碰撞体，从没量过）
- [ ] 队友乘车（bot 乘客上你的车，一车压山头，到达同步终极解）
- [ ] 山顶占点（进攻方第二胜利条件：站区 X 秒获胜，回合有高潮）
- [ ] M249 独立枪模（现与步枪共用一把）
- [ ] GLB 角色 + 动画状态机（工作量大一档；资源 Quaternius/Poly Pizza CC0）
- [ ] bot 使用投掷物（现在烟/雷是玩家独享）
- [ ] 打 release 标签 v0.1.0 存档当前稳定版

## 已完成（归档）

- [x] 掩体系统（石/树挡子弹、岩石挡视线、路线掩体链）
- [x] 载具（沙滩车 + 司机视角 + 引擎声）
- [x] 击倒/击杀横幅、枪声方位弧、圆形小地图
- [x] 攻守平衡（大样本：扇区防守，进攻 ~53%）
- [x] 手榴弹/烟雾弹、背包 + 急救箱/全能医疗箱、倒计时圈
- [x] 载具战斗三件套（撞人碾压 / 600 血可打爆 / 爆炸波及乘员）
- [x] 命名 Uphill Battle · 攻山，GitHub 仓库 + Pages 上线（aleonchen.work/uphill-battle）
